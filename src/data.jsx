export const initialUserData = {
    userData: {
      rows:[],
      columns:[],
    },
    filteredUsers: {
      rows:[],
      columns:[],
    },
  };

export const evalData = {};

export const query = '';

export const flag = true;